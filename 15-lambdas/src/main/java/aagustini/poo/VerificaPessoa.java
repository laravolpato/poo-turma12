package aagustini.poo;

public interface VerificaPessoa {
    // Interface para filtrar uma pessoa
    // (é denominada interface FUNCIONAL, pois possui apenas um método)
    boolean testar(Pessoa p);
}
